<!--
The data-toggle-class attribute tells this button
to toggle the class `is-active` on itself and its target on click.

The data-toggle-escape attribute tells that you can use
the escape key to toggle back off this trigger.

You will probably add data-toggle-target attribute with the ID
of your navigation bar to toggle it at the same time.
-->
<button
	type="button"
	class="example-nav-trigger"
	data-toggle-class
	data-toggle-escape
	title="Navigation">

	<svg width="1em" class="example-nav-trigger-icon" viewBox="0 0 100 100" role="img">
		<title>Navigation icon</title>
		<rect class="top-bar" x="0" y="15" width="100" height="12"/>
		<rect class="mid-bar" x="0" y="44" width="100" height="12"/>
		<rect class="bot-bar" x="0" y="73" width="100" height="12"/>
	</svg>

</button>
